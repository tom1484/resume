{
  "name": "@resume/pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Job pipeline worker: migrations, parse-jd, scoring, notifications",
  "scripts": {
    "migrate": "node src/migrate.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.102.0",
    "ajv": "^8.20.0",
    "pg": "^8.16.0",
    "zod": "^4.4.3"
  }
}
